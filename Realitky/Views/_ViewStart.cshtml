@{
    Layout = "_Layout";
    switch (ViewData["page"])
    {
        case "Admin":
            Layout = "_AdminLayout";
            break;
        default:
            Layout = "_Layout";
            break;
    }
}

@*Remove => nest views*@
